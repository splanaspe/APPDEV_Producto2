import { FilternombrePipe } from './filternombre.pipe';

describe('FilternombrePipe', () => {
  it('create an instance', () => {
    const pipe = new FilternombrePipe();
    expect(pipe).toBeTruthy();
  });
});
